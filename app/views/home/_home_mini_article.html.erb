<div class="col-xs-4">
      <% if article.first_image %>
        <%= retina_image_tag(article.first_image.data_url(:threehundred).to_s, alt: strip_tags((article.first_image.try('caption') ? article.first_image.caption : article.title) + ' ' + (article.first_image.try('credit') ? article.first_image.credit : '')), title: strip_tags((article.first_image.try('caption') ? article.first_image.caption : article.title) + ' ' + (article.first_image.try('credit') ? article.first_image.credit : '')), width: '100%') %>
      <% else %>
        <%= retina_image_tag('fallback/threehundred_no_image.jpg', alt: strip_tags((article.first_image.try('caption') ? article.first_image.caption : article.title) + ' ' + (article.first_image.try('credit') ? article.first_image.credit : '')), title: strip_tags((article.first_image.try('caption') ? article.first_image.caption : article.title) + ' ' + (article.first_image.try('credit') ? article.first_image.credit : '')), width: '100%') %>
      <% end %>
        <h6><%= article.title %></h6>
      </div>