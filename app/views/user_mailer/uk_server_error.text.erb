New Internationalist Magazine - digital subscription
====================================================

Uh oh, looks like the UK login server is throwing an error. This doesn't mean AUS users can't login, just that the user wasn't on our system and then when it tried to reach the UK server it couldn't because of an SSL error.

The error is:

<%= @error %>

<%= render :partial => "footer" %>
